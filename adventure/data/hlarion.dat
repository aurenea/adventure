dependencies {
    // if module has dependencies on any other modules,
    // they should be listed here
}

files {
    objects/hla_objects.dat
}

options {
    keys {
        UP = KEY_UP,
        DOWN = KEY_DOWN,
        LEFT = KEY_LEFT,
        RIGHT = KEY_RIGHT
    }
}
